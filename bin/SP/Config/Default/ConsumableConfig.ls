config.consumable =
{
    pumpPipe =
    {
        cycle = 36,
        lastTime = _G.os.time(),
    },
    solidHalogenBottle =
    {
        cycle = 6,
        lastTime = _G.os.time(),
    },
    catalyst =
    {
        cycle = 12,
        lastTime = _G.os.time(),
    },
}
